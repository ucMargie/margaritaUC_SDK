import { CategoryData, DefaultData } from './interfaces';
import { DpsJsonResponse, LegacyHashService } from '../../Api/interfaces';
declare class Default implements DefaultData {
    categories: CategoryData[];
    controllerId: string;
    constructor(dpsJson: DpsJsonResponse, controllerId: string, legacyHashServices?: LegacyHashService[]);
}
export default Default;
