import { CcpaButtonsConfig, CcpaFirstLayerConfig, CcpaSecondLayerConfig, CcpaUI } from './interfaces';
import { CoreJsonResponse, LegacySettings } from '../../Api/interfaces';
import Common from './Common';
declare class Ccpa extends Common implements CcpaUI {
    buttons: CcpaButtonsConfig;
    firstLayer: CcpaFirstLayerConfig;
    secondLayer: CcpaSecondLayerConfig;
    constructor(coreJson: CoreJsonResponse, legacySettings: LegacySettings);
}
export default Ccpa;
