import { ButtonColor, CommonButtonsCustomization } from '../interfaces';
import { LegacySettings } from '../../../Api/interfaces';
declare class ButtonsCustomization implements CommonButtonsCustomization {
    accept: ButtonColor;
    deny: ButtonColor;
    constructor(legacySettings: LegacySettings);
}
export default ButtonsCustomization;
