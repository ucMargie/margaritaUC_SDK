import { ButtonsCustomization, SecondLayerUrl } from '../interfaces';
import { BUTTON_ALIGNMENT } from '../../enums';
import { LegacySettings } from '../../../Api/interfaces';
declare class ButtonsUI implements ButtonsCustomization {
    alignment: BUTTON_ALIGNMENT;
    borderRadius: string;
    secondLayer: SecondLayerUrl | null;
    constructor(legacySettings: LegacySettings);
}
export default ButtonsUI;
