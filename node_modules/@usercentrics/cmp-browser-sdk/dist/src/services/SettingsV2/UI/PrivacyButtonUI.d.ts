import { CoreJsonResponse } from '../../Api/interfaces';
import { CornerPosition } from '../Core/interfaces';
import { PrivacyButtonUI } from './interfaces';
declare class PrivacyButton implements PrivacyButtonUI {
    backgroundColor: string | null;
    desktopSize: number | null;
    iconColor: string | null;
    iconUrl: string | null;
    mobileSize: number | null;
    position: CornerPosition;
    constructor(coreJson: CoreJsonResponse);
}
export default PrivacyButton;
