import { CoreJsonResponse, LegacySettings } from '../../Api/interfaces';
import { TCFButtonsConfig, TCFFirstLayerConfig, TCFSecondLayerConfig, TcfUI } from './interfaces';
import Common from './Common';
declare class Tcf extends Common implements TcfUI {
    buttons: TCFButtonsConfig;
    firstLayer: TCFFirstLayerConfig;
    secondLayer: TCFSecondLayerConfig;
    constructor(coreJson: CoreJsonResponse, legacySettings: LegacySettings);
}
export default Tcf;
