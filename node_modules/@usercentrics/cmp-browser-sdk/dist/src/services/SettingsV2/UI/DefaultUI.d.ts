import { CoreJsonResponse, LegacySettings } from '../../Api/interfaces';
import { DefaultButtonsConfig, DefaultFirstLayerConfig, DefaultSecondLayerConfig, DefaultUI } from './interfaces';
import Common from './Common';
declare class Default extends Common implements DefaultUI {
    buttons: DefaultButtonsConfig;
    firstLayer: DefaultFirstLayerConfig;
    secondLayer: DefaultSecondLayerConfig;
    constructor(coreJson: CoreJsonResponse, legacySettings: LegacySettings);
}
export default Default;
