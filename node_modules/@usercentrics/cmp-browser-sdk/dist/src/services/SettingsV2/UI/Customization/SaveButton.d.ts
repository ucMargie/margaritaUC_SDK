import { ButtonColor, SaveButtonCustomization } from '../interfaces';
import { LegacySettings } from '../../../Api/interfaces';
declare class SaveButton implements SaveButtonCustomization {
    save: ButtonColor;
    constructor(legacySettings: LegacySettings);
}
export default SaveButton;
